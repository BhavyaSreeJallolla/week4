let p = true;
let q = false;

console.log("p && q:", p && q); //  (AND)
console.log("p || q:", p || q); // (OR)
console.log("!p:", !p);         //  (NOT)
console.log("!(p && q):", !(p && q)); // (NOT AND)
